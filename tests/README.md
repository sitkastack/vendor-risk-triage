# Tests

Unit and integration tests for the agent and its supporting code will live here. Expect pytest as the runner and a clear separation between fast unit tests (run on every commit) and slower integration tests that exercise the agent against fixtures.

Empty through Phases 0, 1, and 2 (documentation phases); populated starting in Phase 3 (Build and Eval) alongside the first agent code.
