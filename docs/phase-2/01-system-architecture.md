# Phase 2: System Architecture

Component-level architecture for the Vendor Risk Triage gate.

## Components

(WIP: ingest layer, document parser, classifier, confidence gate, HITL queue, output writer, audit log.)

## Data flow

(WIP: Mermaid diagram showing vendor document path from ingestion through classification to output. Include trust boundary annotations.)

## Storage

(WIP: Postgres tables, retention policies, archival strategy.)

## Integration points

(WIP: Claude API calls, GRC platform export patterns (AuditBoard, ServiceNow GRC), audit log sinks.)

## External dependencies

(WIP: third-party services, versioning, fallback handling.)
